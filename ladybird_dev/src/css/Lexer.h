#pragma once
// CSS Lexer stub — full tokenisation per CSS Syntax Level 3 (§4).
// For now the Parser handles its own tokenisation inline.
// This header exists as the planned home for a dedicated lexer.
namespace css { }
